# Define default values for customization
appVersion: 0.1.9
replicas: 2
image:
  name: nodejs-backend
service:
  type: LoadBalancer  # Adjust service type (e.g. NodePort) if needed
configMap:
  PORT: 3000  # Listening port
  MONGODB_URI: mongodb:27017/vdt2024
secrets:
  MONGODB_USERNAME: bW9uZ29hZG1pbg==
  MONGODB_PASSWORD: c2VjdXJlcGFzc3dvcmQ=
# Add values for ConfigMap and Secret data if applicable
